'use client'

import React from "react";

const AddToCard = () => {

    return (
        <div>
            <button onClick={() => console.log('click')}>add</button>
        </div>
    )
}

export default AddToCard